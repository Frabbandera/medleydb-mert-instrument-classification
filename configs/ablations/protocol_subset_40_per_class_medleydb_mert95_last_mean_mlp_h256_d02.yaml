experiment_id: protocol_subset_40_per_class_medleydb_mert95_last_mean_mlp_h256_d02
approach: frozen_embeddings
notes: Small balanced subset ablation with 40 segments per class; requires rebuilding
  metadata and cache.
seed: 42
data:
  subset_profile: subset_40_per_class
  label_granularity: medleydb_instrument
  subset_csv: data/metadata/ablations/subset_40_per_class/subset_40_per_class_medleydb_instrument.csv
  label_to_id: data/metadata/ablations/subset_40_per_class/labels_subset_40_per_class_medleydb_instrument_label_to_id.json
  cache_dir: data/cache/mert_v1_95m/ablations/subset_40_per_class/medleydb_instrument/layer_last_pool_mean
  batch_size: 32
  num_workers: 0
  sampler: shuffle
  subset_strategy: balanced
model:
  model_name: m-a-p/MERT-v1-95M
  model_revision: main
  classifier_type: mlp
  hidden_dim: 256
  dropout: 0.2
  layer_aggregation: none
representation:
  layer: last
  pooling: mean
training:
  learning_rate: 0.001
  weight_decay: 0.01
  max_epochs: 30
  early_stopping_patience: 8
  accelerator: auto
  devices: 1
  precision: 32-true
  deterministic: true
  loss_weighting: none
output:
  registry_path: results/experiment_registry.csv
