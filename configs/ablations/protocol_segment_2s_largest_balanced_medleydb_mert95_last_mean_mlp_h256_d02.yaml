experiment_id: protocol_segment_2s_largest_balanced_medleydb_mert95_last_mean_mlp_h256_d02
approach: frozen_embeddings
notes: 2-second segment-length ablation; requires rebuilding the 2 s subset CSV and
  extracting its own cache.
seed: 42
data:
  subset_profile: largest_balanced
  label_granularity: medleydb_instrument
  subset_csv: data/metadata/ablations/segment_2s/subset_largest_balanced_medleydb_instrument_2s.csv
  label_to_id: data/metadata/ablations/segment_2s/labels_largest_balanced_medleydb_instrument_label_to_id.json
  cache_dir: data/cache/mert_v1_95m/ablations/segment_2s/medleydb_instrument/layer_last_pool_mean
  batch_size: 32
  num_workers: 0
  sampler: shuffle
  subset_strategy: balanced
model:
  model_name: m-a-p/MERT-v1-95M
  model_revision: main
  classifier_type: mlp
  hidden_dim: 256
  dropout: 0.2
  layer_aggregation: none
representation:
  layer: last
  pooling: mean
training:
  learning_rate: 0.001
  weight_decay: 0.01
  max_epochs: 30
  early_stopping_patience: 8
  accelerator: auto
  devices: 1
  precision: 32-true
  deterministic: true
  loss_weighting: none
output:
  registry_path: results/experiment_registry.csv
